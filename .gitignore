# ── Byte-compiled / cached ──
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg
.eggs/

# ── Virtual environments ──
.venv/
venv/
env/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ──
.DS_Store
Thumbs.db

# ── Env files ──
.env
.env.local
.env.*.local

# ── ML artifacts ──
*.onnx
*.tflite
*.pt
*.pth
*.pkl
*.pickle
checkpoints/
models/*.onnx
models/*.tflite
models/**/*.onnx
models/**/*.onnx.data
*.onnx.data
# fetched via `make fetch-models`
models/mediapipe/
!models/.gitkeep
# small, describes the bundle's vocabulary
!models/**/labels.json

# ── MLflow ──
mlruns/
mlartifacts/
mlflow.db

# ── DVC ──
/data
*.dvc.lock

# ── Logs ──
logs/
*.log

# ── Coverage ──
.coverage
htmlcov/
.pytest_cache/
.hypothesis/
.benchmarks/

# ── SQLite DBs ──
*.db

# ── Node (web app) ──
node_modules/
.next/
out/
apps/web/.next/
apps/web/node_modules/

# ── Flutter ──
apps/mobile/.dart_tool/
apps/mobile/build/

# ── Tauri ──
apps/desktop/src-tauri/target/

# ── Web runtime assets (restored by `npm run sync-runtime`) ──
# Large WASM binaries that ship inside npm packages. Keeping them in git would
# add ~170MB of history for files that `npm install` already provides.
apps/web/public/onnx/*.wasm
apps/web/public/onnx/*.mjs
apps/web/public/onnx/mediapipe/
# Trained model bundles are build artifacts; only their label manifest is tracked.
apps/web/public/onnx/**/*.onnx
apps/web/public/onnx/**/*.onnx.data
!apps/web/public/onnx/**/labels.json

# TypeScript incremental build cache
*.tsbuildinfo
