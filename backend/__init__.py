"""Dextera AI — FastAPI backend (optional server mode).

This package provides REST + WebSocket endpoints for remote inference.
It is OPTIONAL — the core ML pipeline runs entirely on-device without this.

All imports reference the root `core/` package, NOT any old backend/ modules.
"""
